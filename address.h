#pragma once

namespace pkodev { namespace address {

	// Game.exe 0 (1222073761)
	namespace GAME_13X_0
	{
		// void CHeadSay::Render(D3DXVECTOR3& pos)
		constexpr unsigned int CHeadSay__Render = 0x00470690;

		// CGuiFont
		constexpr unsigned int CGuiFont = 0x0066A730;

		// void CGuiFont::BRender(unsigned int nIndex, const char* str, int x, int y, DWORD color, DWORD shadow)
		constexpr unsigned int CGuiFont__BRender = 0x0046FF70;

		// MPRender
		constexpr unsigned int MPRender = 0x006007DC;

		// BOOL MPRender::WorldToScreen(float fX, float fY, float fZ, int *pnX, int *pnY)
		constexpr unsigned int MPRender__WorldToScreen = 0x00600658;

		// CMPFont
		constexpr unsigned int CMPFont = 0x0066A740;

		// SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
		constexpr unsigned int CMPFont__GetTextSize = 0x00600710;
	}

	// Game.exe 1 (1243412597)
	namespace GAME_13X_1
	{
		// void CHeadSay::Render(D3DXVECTOR3& pos)
		constexpr unsigned int CHeadSay__Render = 0x004706D0;

		// CGuiFont
		constexpr unsigned int CGuiFont = 0x0066A730;

		// void CGuiFont::BRender(unsigned int nIndex, const char* str, int x, int y, DWORD color, DWORD shadow)
		constexpr unsigned int CGuiFont__BRender = 0x0046FFB0;

		// MPRender
		constexpr unsigned int MPRender = 0x006007DC;

		// BOOL MPRender::WorldToScreen(float fX, float fY, float fZ, int *pnX, int *pnY)
		constexpr unsigned int MPRender__WorldToScreen = 0x00600658;

		// CMPFont
		constexpr unsigned int CMPFont = 0x0066A740;

		// SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
		constexpr unsigned int CMPFont__GetTextSize = 0x00600710;
	}

	// Game.exe 2 (1252912474)
	namespace GAME_13X_2
	{
		// void CHeadSay::Render(D3DXVECTOR3& pos)
		constexpr unsigned int CHeadSay__Render = 0x00470770;

		// CGuiFont
		constexpr unsigned int CGuiFont = 0x0066A730;

		// void CGuiFont::BRender(unsigned int nIndex, const char* str, int x, int y, DWORD color, DWORD shadow)
		constexpr unsigned int CGuiFont__BRender = 0x00470050;

		// MPRender
		constexpr unsigned int MPRender = 0x006007DC;

		// BOOL MPRender::WorldToScreen(float fX, float fY, float fZ, int *pnX, int *pnY)
		constexpr unsigned int MPRender__WorldToScreen = 0x00600658;

		// CMPFont
		constexpr unsigned int CMPFont = 0x0066A740;

		// SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
		constexpr unsigned int CMPFont__GetTextSize = 0x00600710;
	}

	// Game.exe 3 (1244511158)
	namespace GAME_13X_3
	{
		// void CHeadSay::Render(D3DXVECTOR3& pos)
		constexpr unsigned int CHeadSay__Render = 0x00470760;

		// CGuiFont
		constexpr unsigned int CGuiFont = 0x0066A730;

		// void CGuiFont::BRender(unsigned int nIndex, const char* str, int x, int y, DWORD color, DWORD shadow)
		constexpr unsigned int CGuiFont__BRender = 0x00470040;

		// MPRender
		constexpr unsigned int MPRender = 0x006007DC;

		// BOOL MPRender::WorldToScreen(float fX, float fY, float fZ, int *pnX, int *pnY)
		constexpr unsigned int MPRender__WorldToScreen = 0x00600658;

		// CMPFont
		constexpr unsigned int CMPFont = 0x0066A740;

		// SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
		constexpr unsigned int CMPFont__GetTextSize = 0x00600710;
	}

	// Game.exe 4 (1585009030)
	namespace GAME_13X_4
	{
		// void CHeadSay::Render(D3DXVECTOR3& pos)
		constexpr unsigned int CHeadSay__Render = 0x004707D0;

		// CGuiFont
		constexpr unsigned int CGuiFont = 0x0066A748;

		// void CGuiFont::BRender(unsigned int nIndex, const char* str, int x, int y, DWORD color, DWORD shadow)
		constexpr unsigned int CGuiFont__BRender = 0x00470150;

		// MPRender
		constexpr unsigned int MPRender = 0x006007DC;

		// BOOL MPRender::WorldToScreen(float fX, float fY, float fZ, int *pnX, int *pnY)
		constexpr unsigned int MPRender__WorldToScreen = 0x00600658;

		// CMPFont
		constexpr unsigned int CMPFont = 0x0066A758;

		// SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
		constexpr unsigned int CMPFont__GetTextSize = 0x00600710;
	}

	// Game.exe 5 (1207214236)
	namespace GAME_13X_5
	{
		// void CHeadSay::Render(D3DXVECTOR3& pos)
		constexpr unsigned int CHeadSay__Render = 0x00470360;

		// CGuiFont
		constexpr unsigned int CGuiFont = 0x0066A738;

		// void CGuiFont::BRender(unsigned int nIndex, const char* str, int x, int y, DWORD color, DWORD shadow)
		constexpr unsigned int CGuiFont__BRender = 0x0046FC40;

		// MPRender
		constexpr unsigned int MPRender = 0x006007DC;

		// BOOL MPRender::WorldToScreen(float fX, float fY, float fZ, int *pnX, int *pnY)
		constexpr unsigned int MPRender__WorldToScreen = 0x00600658;

		// CMPFont
		constexpr unsigned int CMPFont = 0x0066A748;

		// SIZE* CMPFont::GetTextSize(char* szText, SIZE* pSize,float fScale = 1.0f);
		constexpr unsigned int CMPFont__GetTextSize = 0x00600710;
	}

}}